const fs = require("fs");
function processData(input) {
  const n = parseInt(input.trim());
  // your code here
  // console.log(ans);
}

const input = fs.readFileSync("/dev/stdin", "utf-8");
processData(input);